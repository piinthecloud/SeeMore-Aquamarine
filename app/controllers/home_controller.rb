class HomeController < ApplicationController
  def index
    # @user = User.find(id: @current_user.user_id)
    # @all_vimeos = @user.feeds.where(social_media: "Vimeo")
    # if @current_user
      # @all_vimeos = @current_user.feeds.where(social_media: "Vimeo")
    # end
  end
end
